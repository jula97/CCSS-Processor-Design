module reg_type3_16bit_test(input clk, 
							  input write_en,
							  input [3:0]datain,
					        input inc,
							  input reset,
					
					        output reg [3:0]dataout);

					
	always @ (posedge clk)
		begin
			if (write_en == 1) 		
				dataout <= datain;
			if (reset == 1)	
			   dataout <= 4'd0; 	
			if (inc == 1)	
			   dataout <= dataout + 1;		
		end

endmodule